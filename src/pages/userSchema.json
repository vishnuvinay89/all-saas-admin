{
  "title": "",
  "description": "",
  "type": "object",
  "required": ["name", "username", "password", "role"],
  "properties": {
    "name": {
      "title": "Full Name",
      "fieldId": null,
      "type": "string",
      "description": "Full name, numbers, letters and spaces are allowed.",
      "validation": []
    },
    "username": {
      "title": "Username",
      "fieldId": null,
      "minLength": 3,
      "pattern": "^[a-zA-Z0-9!@#$%^&*()_+={}:;<>?,./~`|\\-]+$",
      "type": "string",
      "description": "Username must be at least 3 characters long.",
      "validation": [],
      "ui:options": {
        "inputProps": {
          "autoComplete": "off"
        }
      }
    },
    "password": {
      "title": "Password",
      "type": "string",
      "description": "Your password needs to include both uppercase & lowercase letters, a number, and a special character. Must be at least 8 characters long.",
      "ui:widget": "password",
      "minLength": 8,
      "pattern": "^(?=.*[a-z])(?=.*[A-Z])(?=.*\\d)(?=.*[!@#$%^&*(),.?\":{}|<>]).{8,}$"
    },
    "role": {
      "title": "Role",
      "type": "string",
      "default": "learner",
      "oneOf": [
        {
          "title": "Learner",
          "const": "learner"
        }
      ],
      "validation": []
    },
    "grade": {
      "title": "Grade",
      "type": "integer",
      "minimum": 1,
      "maximum": 10,
      "validation": ["required"],
      "ui:widget": "number",
      "ui:placeholder": "Enter grade (1-10)"
    }
  },
  "dependencies": {}
}
